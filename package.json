{
  "name": "ceramic-blog",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build": "lerna run build",
    "prepare": "lerna run prepare"
  },
  "devDependencies": {
    "lerna": "^4.0.0"
  }
}
